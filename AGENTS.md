# AGENTS.md

## Git rules

- NEVER run `git commit` or `git push` on your own. The user commits manually.
- You may inspect the repo (`git status`, `git diff`, `git log`) but never stage, commit, or push.
- Do not delete tags or branches unless the user explicitly asks.
